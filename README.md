# Anna DL application

A special application that downloads only documents legally provided for download.

Install it as a python application in a virtual environment

Install the requirements for requirements.in
Use it like this:

python anna-dl <local path to place the file> --s <search query in quotes> --all

